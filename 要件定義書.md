# 運転サポートシステム 要件定義書

**プロジェクト名:** Driving Support System  
**作成日:** 2026年2月10日  
**バージョン:** 1.1  

---

## 1. プロジェクト概要

### 1.1 目的

運転中の居眠りを検知し、ドライバーの安全を支援するシステムを構築する。  
カメラで取得した顔映像から目の開閉状態をリアルタイムに判定し、一定時間以上目を閉じた場合にアラームで警告する。  
また、運転業務を補助するカウンター機能を併設する。

### 1.2 システム構成

| 項目 | 内容 |
|------|------|
| メインデバイス | Raspberry Pi 4 Model B |
| ディスプレイ | 7インチ タッチモニター（800×480） |
| カメラ | USBカメラ または Raspberry Pi Camera Module |
| 開発言語 | Python 3.11 |
| 映像処理 | MediaPipe Face Landmarker（Tasks API） + OpenCV |
| 動作環境 | Raspberry Pi OS（64bit）/ Windows / macOS |

---

## 2. 機能要件

### 2.1 【最重要】居眠り検知・アラーム機能

#### 概要

カメラ映像からリアルタイムに顔のランドマーク（478点）を検出し、目の開閉状態を EAR（Eye Aspect Ratio：目のアスペクト比）で判定する。**1秒以上連続で目を閉じた場合**にアラーム音を鳴動する。

> **重要:** アラームは「検知モード」が ON のときのみ鳴動する。検知モード OFF 時はカメラ映像・EAR 算出は継続するがアラームは発生しない。

#### EAR（Eye Aspect Ratio）の算出

```
EAR = (||p2 - p6|| + ||p3 - p5||) / (2 × ||p1 - p4||)
```

MediaPipe Face Landmarker（478点モデル）における目のランドマーク番号：

| 部位 | p1 | p2 | p3 | p4 | p5 | p6 |
|------|-----|-----|-----|-----|-----|-----|
| 左目 | 33 | 159 | 145 | 133 | 153 | 144 |
| 右目 | 263 | 386 | 374 | 362 | 380 | 373 |

#### 判定ロジック

```
if 検知モード == OFF:
    映像表示のみ（アラーム判定しない）
    return

両目のEAR平均値 = (左目EAR + 右目EAR) / 2

if 両目のEAR平均値 < 閾値（校正値 or デフォルト値）:
    目を閉じていると判定
    目の周りを緑色のランドマーク線で描画
    閉じ始め時刻を記録
    if 連続閉眼時間 >= 1.0秒:
        アラーム鳴動
else:
    目を開いていると判定
    目の周りを緑色のランドマーク線で描画
    閉じ始め時刻をリセット
    アラーム停止
```

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-EYE-001 | カメラ映像から顔を検出し、目のランドマークを取得する | 必須 |
| F-EYE-002 | 左右の目それぞれの EAR を算出する | 必須 |
| F-EYE-003 | **検知モード ON 時のみ**、両目の EAR 平均が閾値を下回った状態が **1秒以上** 継続した場合、アラーム音を鳴動する | 必須 |
| F-EYE-004 | 目を開けたらアラームを即座に停止する | 必須 |
| F-EYE-005 | 画面上に目の開閉状態（OPEN / CLOSED）をリアルタイム表示する | 必須 |
| F-EYE-006 | **検知モード中はカメラ映像上の目の周りに緑色のランドマーク線を描画する** | 必須 |
| F-EYE-007 | 画面上に現在の EAR 値をリアルタイム表示する（デバッグ・調整用） | 推奨 |
| F-EYE-008 | 顔が検出されない場合、「顔が検出されません」と画面に警告表示する | 推奨 |
| F-EYE-009 | アラーム音量を設定可能にする | 任意 |

---

### 2.2 校正（キャリブレーション）機能

#### 概要

個人差（目の大きさ・形状）に対応するため、開眼時・閉眼時それぞれの EAR 値を測定し、最適な閾値を自動算出する。

#### 校正フロー

```
[校正ボタン押下]
    │
    ▼
[画面表示] "目を開けてください"
    │
    ▼
[カウントダウン] 3 → 2 → 1（各秒ビープ音）
    │
    ▼
[開眼EAR測定] ← 数フレーム分の平均値を取得
    │
    ▼
[画面表示] "目を閉じてください"
    │
    ▼
[カウントダウン] 3 → 2 → 1（各秒ビープ音）
    │
    ▼
[閉眼EAR測定] ← 数フレーム分の平均値を取得
    │
    ▼
[閾値算出] threshold = (開眼EAR + 閉眼EAR) / 2
    │
    ▼
[画面表示] "校正完了！"
[校正値を保存]
```

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-CAL-001 | 「校正」ボタンを押すと校正モードに入る | 必須 |
| F-CAL-002 | 「目を開けてください」と画面に表示する | 必須 |
| F-CAL-003 | 3秒のカウントダウンを画面表示し、毎秒ビープ音を鳴らす | 必須 |
| F-CAL-004 | カウントダウン後に開眼状態の EAR 平均値を測定・記録する | 必須 |
| F-CAL-005 | 「目を閉じてください」と画面に表示する | 必須 |
| F-CAL-006 | 再度3秒のカウントダウンを画面表示し、毎秒ビープ音を鳴らす | 必須 |
| F-CAL-007 | カウントダウン後に閉眼状態の EAR 平均値を測定・記録する | 必須 |
| F-CAL-008 | 開眼・閉眼の EAR 中間値を閾値として自動算出する | 必須 |
| F-CAL-009 | 校正値をファイル（JSON等）に保存し、次回起動時に読み込む | 必須 |
| F-CAL-010 | 校正完了後「校正完了！」と画面に表示する | 必須 |
| F-CAL-011 | 校正未実施の場合はデフォルト閾値（0.2 目安）を使用する | 必須 |

---

### 2.3 カウンター機能

#### 概要

タッチ操作で数値カウントを行う汎用カウンター。運転業務中の回数記録等に使用する。

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-CNT-001 | カウンター値を画面に大きく表示する | 必須 |
| F-CNT-002 | 「＋」ボタンを押すとカウンターが +1 される | 必須 |
| F-CNT-003 | 「−」ボタンを押すとカウンターが -1 される | 必須 |
| F-CNT-004 | 「リセット」ボタンを押すとカウンターが 0 に戻る | 必須 |
| F-CNT-005 | カウンターはマイナス値も許容する | 推奨 |
| F-CNT-006 | ボタンはタッチモニターで操作しやすいサイズにする（最低 60×60px） | 必須 |

---

### 2.4 検知モード機能

#### 概要

「検知開始」ボタンで居眠り検知を ON/OFF する。検知中のみアラームが作動し、それ以外では映像表示のみでアラームは鳴らない。

#### 状態遷移

```
[待機中（STANDBY）]
    │  「検知開始」ボタン押下
    ▼
[検知中（DETECTING）]
    │  ・目のランドマークを緑色で描画
    │  ・1秒以上閉眼でアラーム鳴動
    │  「検知停止」ボタン押下
    ▼
[待機中（STANDBY）]
    │  ・映像表示のみ
    │  ・アラーム無効
```

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-DET-001 | 「検知開始」ボタンを押すと検知モードが ON になる | 必須 |
| F-DET-002 | 検知モード ON 中は「検知停止」ボタンに表示が切り替わる | 必須 |
| F-DET-003 | 検知モード OFF 時はアラーム判定を行わない | 必須 |
| F-DET-004 | 検知モード ON 時は目の周りに緑色のランドマーク線を描画する | 必須 |
| F-DET-005 | ステータスバーに現在の状態（「待機中」「検知中」「校正中」等）を表示する | 必須 |
| F-DET-006 | 検知モード切替時にビープ音等で状態変更を通知する | 推奨 |

---

### 2.5 シャットダウン機能

#### 概要

タッチ操作で Raspberry Pi を安全にシャットダウンする。誤操作防止のため確認ダイアログを表示する。

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-SHD-001 | 「シャットダウン」ボタンを画面に配置する | 必須 |
| F-SHD-002 | ボタン押下時に「本当にシャットダウンしますか？」の確認ダイアログを表示する | 必須 |
| F-SHD-003 | 確認後、カメラ・MediaPipe リソースを解放してから `sudo shutdown -h now` を実行する | 必須 |
| F-SHD-004 | PC 環境ではシャットダウンではなくアプリケーション終了のみ行う | 必須 |
| F-SHD-005 | シャットダウンボタンは誤タッチしにくい位置・サイズに配置する | 推奨 |

---

### 2.6 ステータス表示機能

#### 概要

画面上部にステータスバーを常時表示し、システムの現在の状態と日時情報を一目で把握できるようにする。

#### 詳細要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-STS-001 | ステータスバーに現在のモード（待機中 / 検知中 / 校正中）を表示する | 必須 |
| F-STS-002 | モードに応じてステータスバーの背景色を変更する（待機中:グレー、検知中:緑、校正中:オレンジ） | 必須 |
| F-STS-003 | ステータスバーに現在の日付と時刻（HH:MM:SS）をリアルタイム表示する | 必須 |
| F-STS-004 | 目の開閉状態（OPEN / CLOSED）をステータスバーまたは情報エリアに表示する | 必須 |
| F-STS-005 | FPS（フレームレート）を表示する（デバッグ用、設定で非表示可） | 推奨 |

---

## 3. 非機能要件

### 3.1 パフォーマンス

| ID | 要件 | 目標値 |
|----|------|--------|
| NF-PERF-001 | フレームレート（Raspberry Pi 4） | 10 FPS 以上 |
| NF-PERF-002 | フレームレート（PC） | 20 FPS 以上 |
| NF-PERF-003 | 閉眼検知からアラーム鳴動までの遅延 | 1.0秒 ± 0.2秒 |
| NF-PERF-004 | CPU使用率（Raspberry Pi 4） | 80% 以下 |

### 3.2 ユーザビリティ

| ID | 要件 |
|----|------|
| NF-UI-001 | 7インチ 800×480 のタッチモニターでの操作に最適化したUIレイアウト |
| NF-UI-002 | ボタン・文字は指でタッチ操作しやすい大きさ（最低 48×48dp 推奨 60×60dp） |
| NF-UI-003 | 運転中でも視認できる文字サイズ・配色（高コントラスト） |
| NF-UI-004 | 日本語表示対応 |

### 3.3 可搬性（PC / Raspberry Pi 共通動作）

| ID | 要件 |
|----|------|
| NF-PORT-001 | 同一コードベースで Raspberry Pi 4 と PC（Windows/macOS/Linux）の両方で動作する |
| NF-PORT-002 | プラットフォーム固有処理（カメラ、サウンド等）は抽象化レイヤーで吸収する |
| NF-PORT-003 | 設定ファイル（config.json）でカメラID・解像度・閾値等を切り替え可能にする |

### 3.4 信頼性

| ID | 要件 |
|----|------|
| NF-REL-001 | カメラ切断時にエラーハンドリングし、再接続を試みる |
| NF-REL-002 | 長時間連続稼働（8時間以上）に耐えるメモリリーク対策 |
| NF-REL-003 | 校正データが破損した場合はデフォルト閾値にフォールバックする |

---

## 4. 技術設計

### 4.1 使用技術・ライブラリ

| ライブラリ | バージョン | 用途 |
|------------|------------|------|
| Python | 3.11 | メイン言語 |
| mediapipe | 0.10.x（最新安定版） | 顔ランドマーク検出（Face Landmarker Tasks API） |
| opencv-python | 4.x | カメラ映像取得・画像処理 |
| numpy | 最新 | 数値計算（EAR算出） |
| tkinter | Python同梱 | GUI（タッチUI） |
| pygame | 2.x | サウンド再生（アラーム・ビープ音） |

### 4.2 MediaPipe API に関する重要な注意

#### 旧API（非推奨）
```python
# ❌ これは旧APIです。使用しないでください。
import mediapipe as mp
mp_face_mesh = mp.solutions.face_mesh
```

#### 新API（Tasks API）— こちらを使用
```python
# ✅ 現在の推奨API
import mediapipe as mp
from mediapipe.tasks import python
from mediapipe.tasks.python import vision

BaseOptions = mp.tasks.BaseOptions
FaceLandmarker = mp.tasks.vision.FaceLandmarker
FaceLandmarkerOptions = mp.tasks.vision.FaceLandmarkerOptions
VisionRunningMode = mp.tasks.vision.RunningMode

options = FaceLandmarkerOptions(
    base_options=BaseOptions(model_asset_path='face_landmarker.task'),
    running_mode=VisionRunningMode.LIVE_STREAM,
    num_faces=1,
    min_face_detection_confidence=0.5,
    min_face_presence_confidence=0.5,
    result_callback=callback_function
)
```

> **ポイント:** モデルファイル `face_landmarker.task` を公式サイトからダウンロードして使用する。  
> ダウンロード先: https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker#models

### 4.3 Raspberry Pi での MediaPipe インストール

#### 現在の状況（2026年2月時点）

| 方法 | 説明 | 推奨 |
|------|------|------|
| `pip install mediapipe` | 公式 PyPI パッケージ。aarch64 対応が公式で進んでいる | ⭕ まず試す |
| PINTO0309/mediapipe-bin | 非公式ビルド済みwheel。v0.8.5（2022年）で更新停止 | △ 公式が動かない時のフォールバック |
| ソースビルド | 公式リポジトリからビルド | △ 最終手段 |

> **重要:** PINTO0309/mediapipe-bin は v0.8.5 で更新が止まっており（旧API時代）、新しい Tasks API に対応していない可能性が高いです。  
> まず公式 `pip install mediapipe` を試し、Raspberry Pi OS 64bit（Bookworm）で動作するか確認してください。  
> もし公式 pip でインストールできない場合は、以下の代替案を検討してください：
> 1. Google公式の Raspberry Pi 用サンプル（https://github.com/google-ai-edge/mediapipe-samples/tree/main/examples/face_landmarker/raspberry_pi）のsetup.shを参考にする
> 2. Python Wheel をソースからビルドする（https://ai.google.dev/edge/mediapipe/solutions/build_python）

### 4.4 PC と Raspberry Pi でのコード共有に関する注意

#### 共有可能な部分（問題なし）

- EAR 算出ロジック
- 校正ロジック・校正データ管理
- カウンター機能
- 設定ファイル管理
- MediaPipe Face Landmarker の呼び出し（同一API）

#### プラットフォーム差異に注意が必要な部分

| 項目 | PC | Raspberry Pi | 対策 |
|------|-----|--------------|------|
| カメラデバイスID | 0（内蔵カメラ）or 外部 | 0（USBカメラ）or PiCamera | config.json でカメラID指定 |
| カメラ解像度 | 高解像度可 | 640×480 推奨（性能面） | config.json で解像度指定 |
| mediapipe インストール | `pip install mediapipe`（問題なし） | 公式pip or 別途対応 | セットアップスクリプトで分岐 |
| GUI描画性能 | 高速 | やや遅い | Raspberry Pi 用にUI軽量化 |
| サウンド再生 | pygame で問題なし | ALSA/PulseAudio 設定が必要な場合あり | pygame + 環境チェック |
| フォント | OS標準 | 日本語フォント要インストール | `fonts-noto-cjk` パッケージ |

### 4.5 システムアーキテクチャ

```
┌─────────────────────────────────────────────────┐
│                    メインループ                    │
│  ┌───────────┐  ┌──────────────┐  ┌───────────┐ │
│  │  カメラ    │→│ MediaPipe    │→│  EAR算出   │ │
│  │  キャプチャ │  │ FaceLandmark │  │  & 判定   │ │
│  └───────────┘  └──────────────┘  └─────┬─────┘ │
│                                         │       │
│  ┌──────────────────────────────────────┘       │
│  │                                               │
│  ▼                                               │
│  ┌───────────┐  ┌──────────────┐  ┌───────────┐ │
│  │ アラーム   │  │  校正機能    │  │ カウンター │ │
│  │ 制御      │  │              │  │           │ │
│  └───────────┘  └──────────────┘  └───────────┘ │
│                                                   │
│  ┌───────────────────────────────────────────┐   │
│  │              GUI（tkinter）                │   │
│  │  ┌────────┐ ┌────────┐ ┌────────────────┐│   │
│  │  ┌────────┐ ┌──────┐ ┌──────────────────┐│   │
│  │  │映像表示│ │状態  │ │ボタン・カウンター││   │
│  │  └────────┘ └──────┘ └──────────────────┘│   │
│  └───────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 4.6 ディレクトリ構成（案）

```
driving-support-system/
├── main.py                  # エントリーポイント
├── config.json              # 設定ファイル（カメラID、解像度、閾値等）
├── calibration_data.json    # 校正データ保存ファイル
├── requirements.txt         # Python依存パッケージ
├── setup_raspi.sh           # Raspberry Pi セットアップスクリプト
├── models/
│   └── face_landmarker.task # MediaPipeモデルファイル
├── src/
│   ├── __init__.py
│   ├── eye_detector.py      # EAR算出・閉眼判定ロジック
│   ├── alarm.py             # アラーム音制御
│   ├── calibration.py       # 校正機能
│   ├── counter.py           # カウンター機能
│   ├── camera.py            # カメラ制御（プラットフォーム抽象化）
│   ├── config.py            # 設定読み込み
│   └── gui.py               # GUI（tkinter）
├── sounds/
│   ├── alarm.wav            # アラーム音
│   └── beep.wav             # カウントダウン用ビープ音
└── README.md
```

---

## 5. UI デザイン（ワイヤーフレーム）

### 5.1 画面レイアウト方針

- **800×480 の限られた画面を最大限活用する**
- 左側にカメラ映像（メインエリア）、右側に操作パネル
- 最上部にステータスバー（現在の状態 + 日時）を常時表示
- ボタンは指タッチに最適な **64×48px 以上**
- 高コントラスト（暗背景 + 明るい文字）で運転中でも視認可能

### 5.2 メイン画面（800×480 タッチモニター想定）

```
┌──────────────────────────────────────────────────────────────┐
│ ● 検知中         EAR: 0.31  👁 OPEN       2026/02/10 14:32:05 │  ← ステータスバー
├───────────────────────────────┬──────────────────────────────┤     (モードで背景色変化)
│                               │                              │
│                               │  ┌────────────────────────┐  │
│                               │  │   検知開始    ▶        │  │  ← メインボタン(大きく)
│    カメラ映像                  │  └────────────────────────┘  │
│    (420×320)                  │                              │
│                               │  ┌────────────────────────┐  │
│    検知中:目の周りに           │  │      校正  🎯          │  │
│    緑色ランドマーク描画        │  └────────────────────────┘  │
│                               │                              │
│                               │  ── カウンター ──────────── │
│                               │  ┌────────────────────────┐  │
│                               │  │          12            │  │  ← カウンター値(大文字)
├───────────────────────────────┤  └────────────────────────┘  │
│  顔検出: OK  FPS: 15         │  ┌──────┐┌──────┐┌────────┐  │
│                               │  │  ＋  ││  −  ││ Reset  │  │
│                               │  └──────┘└──────┘└────────┘  │
│                               │                              │
│                               │  ┌────────────────────────┐  │
│                               │  │  ⏻ シャットダウン      │  │  ← 下部に配置(誤操作防止)
│                               │  └────────────────────────┘  │
└───────────────────────────────┴──────────────────────────────┘
```

### 5.3 ステータスバー詳細

```
┌──────────────────────────────────────────────────────────────┐
│ ● [モード表示]     [EAR値]  [目の状態]       [日付 時刻]      │
└──────────────────────────────────────────────────────────────┘

各モードでの表示と背景色:
  待機中:   ● 待機中        (背景: グレー   #555555)
  検知中:   ● 検知中        (背景: 緑     #228B22)
  校正中:   ● 校正中        (背景: オレンジ #FF8C00)
  アラーム: ● ⚠ 警告！      (背景: 赤     #CC0000) ← 閉眼1秒超過時
```

### 5.4 検知モード ON 時のカメラ映像

```
┌───────────────────────────────┐
│                               │
│        ┌─────────┐            │
│        │  顔検出  │            │
│        │         │            │
│        │ ╭───╮   ╭───╮      │
│        │ │左目│   │右目│      │  ← 緑色のランドマーク線
│        │ ╰───╯   ╰───╯      │     で目の輪郭を描画
│        │         │            │
│        └─────────┘            │
│                               │
└───────────────────────────────┘
```

### 5.5 校正画面（オーバーレイ表示）

```
┌──────────────────────────────────────────────────────────────┐
│ ● 校正中                                   2026/02/10 14:33:00 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                                                              │
│               目を開けてください                               │
│                                                              │
│                      3                                       │
│                 (カウントダウン)                               │
│                                                              │
│                                                              │
│                                                              │
│                           ┌──────────────────┐               │
│                           │   キャンセル ✕   │               │
│                           └──────────────────┘               │
└──────────────────────────────────────────────────────────────┘
```

### 5.6 シャットダウン確認ダイアログ

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│          ┌──────────────────────────────────┐                │
│          │                                  │                │
│          │   本当にシャットダウンしますか？    │                │
│          │                                  │                │
│          │   ┌──────────┐  ┌──────────┐    │                │
│          │   │   はい   │  │ いいえ   │    │                │
│          │   └──────────┘  └──────────┘    │                │
│          │                                  │                │
│          └──────────────────────────────────┘                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. 推奨環境・バージョン

### 6.1 Raspberry Pi

| 項目 | 推奨 |
|------|------|
| ハードウェア | Raspberry Pi 4 Model B（**RAM 4GB 以上推奨**） |
| OS | Raspberry Pi OS **64bit Bookworm**（2024年以降リリース版） |
| Python | 3.11（Bookworm 同梱） |
| カメラ | USB Webカメラ（Logicool C270 等）推奨。Pi Camera Module v2 も可 |
| ディスプレイ | 7インチ タッチモニター 800×480 |

> **RAM について:** MediaPipe + OpenCV + GUI を同時に動かすため、2GB モデルでは不足する可能性があります。**4GB 以上を強く推奨**します。

> **OS について:** Bookworm（64bit）を推奨する理由：
> - Python 3.11 が標準搭載
> - aarch64 対応パッケージが豊富
> - MediaPipe 公式 pip パッケージの互換性が高い
> - Bullseye 以前は古い glibc のため依存問題が起きやすい

### 6.2 PC（開発・テスト用）

| 項目 | 推奨 |
|------|------|
| OS | Windows 10/11, macOS, Ubuntu 22.04+ |
| Python | 3.11（Raspberry Pi と合わせる） |
| カメラ | 内蔵 or USB Webカメラ |

### 6.3 仮想環境

```bash
# PCでもRaspberry Piでも共通
python3 -m venv venv
source venv/bin/activate  # Linux/macOS/Raspberry Pi
# venv\Scripts\activate   # Windows

pip install -r requirements.txt
```

---

## 7. 設定ファイル（config.json）設計

```json
{
  "camera": {
    "device_id": 0,
    "width": 640,
    "height": 480,
    "fps": 30
  },
  "eye_detection": {
    "default_ear_threshold": 0.2,
    "closed_duration_threshold_sec": 1.0,
    "ear_smoothing_frames": 3
  },
  "calibration": {
    "countdown_sec": 3,
    "measurement_frames": 15
  },
  "alarm": {
    "sound_file": "sounds/alarm.wav",
    "beep_file": "sounds/beep.wav",
    "volume": 0.8
  },
  "ui": {
    "fullscreen": false,
    "window_width": 800,
    "window_height": 480
  }
}
```

---

## 8. セットアップ手順（概要）

### 8.1 Raspberry Pi

```bash
# 1. OS更新
sudo apt update && sudo apt upgrade -y

# 2. 必要パッケージのインストール
sudo apt install -y python3-venv python3-pip python3-dev
sudo apt install -y libatlas-base-dev  # numpy用
sudo apt install -y fonts-noto-cjk     # 日本語フォント
sudo apt install -y libsdl2-mixer-2.0-0  # pygame用

# 3. プロジェクトセットアップ
cd ~/driving-support-system
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

# 4. モデルファイルのダウンロード
cd models
wget -O face_landmarker.task \
  https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/latest/face_landmarker.task

# 5. 起動
cd ..
python main.py
```

### 8.2 PC

```bash
# 1. プロジェクトセットアップ
cd driving-support-system
python -m venv venv
# Windows: venv\Scripts\activate
# macOS/Linux: source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

# 2. モデルファイルのダウンロード（初回のみ）
# models/face_landmarker.task をダウンロード

# 3. 起動
python main.py
```

---

## 9. requirements.txt

```
mediapipe>=0.10.9
opencv-python>=4.8.0
numpy>=1.24.0
pygame>=2.5.0
```

> **Raspberry Pi 注意:** `opencv-python` の代わりに `opencv-python-headless` が必要になる場合があります。また、Raspberry Pi OS の場合 `--break-system-packages` フラグや `venv` の利用が必要です。

---

## 10. リスク・課題

| # | リスク・課題 | 影響度 | 対策 |
|---|-------------|--------|------|
| 1 | Raspberry Pi 4 で mediapipe の公式 pip インストールが失敗する | 高 | Google公式 RPi サンプル（setup.sh）を参考にする。最悪ソースビルド |
| 2 | Raspberry Pi 4 での処理速度が不足（FPS低下） | 高 | カメラ解像度を下げる（320×240）、顔検出の信頼度閾値を調整、不要な描画を減らす |
| 3 | 暗所・逆光で顔検出精度が低下 | 中 | IR カメラの検討、検出信頼度閾値の引き下げ、LED補助照明 |
| 4 | タッチモニターのドライバ問題 | 中 | 購入前に Raspberry Pi OS 対応を確認、事前テスト |
| 5 | 個人差（メガネ・サングラス）で EAR 精度低下 | 中 | 校正機能で個人差を吸収、メガネ非対応の場合は注意表示 |
| 6 | PINTO0309/mediapipe-bin が古く Tasks API 未対応 | 中 | 公式 pip を優先。非対応の場合はソースビルトまたはLegacy APIフォールバック |
| 7 | pygame のサウンド再生が Raspberry Pi で動作しない | 低 | ALSA 設定確認、代替として `aplay` コマンドを使用 |
| 8 | 長時間稼働でのメモリリーク | 中 | 定期的なリソース解放、メモリ監視ログ |

---

## 11. 開発フェーズ（案）

| フェーズ | 内容 | 期間目安 |
|---------|------|---------|
| Phase 1 | PC上で MediaPipe Face Landmarker を使った目検出の PoC（概念実証） | 2〜3日 |
| Phase 2 | EAR 算出 + 閾値判定 + アラーム鳴動の実装 | 2〜3日 |
| Phase 3 | 校正機能の実装 | 1〜2日 |
| Phase 4 | カウンター機能の実装 | 0.5日 |
| Phase 5 | GUI（tkinter）の統合・タッチ最適化 | 2〜3日 |
| Phase 6 | Raspberry Pi 実機テスト・パフォーマンスチューニング | 2〜3日 |
| Phase 7 | 総合テスト・バグ修正 | 1〜2日 |

---

## 12. 参考リンク

| リソース | URL |
|---------|-----|
| MediaPipe Face Landmarker 公式ドキュメント | https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker |
| MediaPipe Face Landmarker Python ガイド | https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker/python |
| MediaPipe Raspberry Pi サンプル | https://github.com/google-ai-edge/mediapipe-samples/tree/main/examples/face_landmarker/raspberry_pi |
| まばたき検知の解説記事（EAR） | https://zenn.dev/ykesamaru/articles/f10804a8fcc81d |
| PINTO0309 mediapipe-bin（参考・旧版） | https://github.com/PINTO0309/mediapipe-bin |
| Face Landmarker モデルダウンロード | https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker#models |
| EAR 論文 "Real-Time Eye Blink Detection using Facial Landmarks" | https://vision.fe.uni-lj.si/cvww2016/proceedings/papers/05.pdf |

---

## 改訂履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2026/02/10 | 1.0 | 初版作成 |
| 2026/02/10 | 1.1 | 検知モード機能追加、シャットダウン機能追加、ステータスバー追加（モード表示・日時表示）、検知中の目ランドマーク緑描画追加、UI全面刷新、解像度800×480確定 |
